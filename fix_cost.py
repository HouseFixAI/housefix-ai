#!/usr/bin/env python3
"""Add cost estimate intent: costs-first UX for 'Kosten Schatting' carousel item."""
p = "/home/team/shared/backend/templates/index.html"
with open(p, "r") as f:
    s = f.read()

# 1. Change "Kosten Schatting" carousel to use openCameraForIntent('cost')
old_carousel = '{bg:"url(https://images.unsplash.com/photo-1581578731548-c64695cc6952?w=800&h=480&fit=crop&fm=webp&q=85)", t:"Kosten Schatting", a:"openCamera()"}'
new_carousel = '{bg:"url(https://images.unsplash.com/photo-1581578731548-c64695cc6952?w=800&h=480&fit=crop&fm=webp&q=85)", t:"Kosten Schatting", a:"openCameraForIntent(\'cost\')"}'
s = s.replace(old_carousel, new_carousel)

# 2. Add "cost" case in renderResults() switch
old_switch = '        switch (intent) {\n          case "purchase":\n            renderColorPalette(data);\n            break;\n          case "find_item":\n            renderFindItem(data);\n            break;\n          case "style_room":\n            renderStyleRoom(data);\n            break;\n          case "style":\n            showInspirationAdvice(data);\n            break;\n          default:\n            showResults(data);\n        }'
new_switch = '        switch (intent) {\n          case "purchase":\n            renderColorPalette(data);\n            break;\n          case "find_item":\n            renderFindItem(data);\n            break;\n          case "style_room":\n            renderStyleRoom(data);\n            break;\n          case "style":\n            showInspirationAdvice(data);\n            break;\n          case "cost":\n            showCostEstimate(data);\n            break;\n          default:\n            showResults(data);\n        }'
s = s.replace(old_switch, new_switch)

# 3. Add showCostEstimate function before the closing } of renderResults
# Find the spot: after the switch closing brace, before the closing brace of renderResults
old_after_switch = '      }\n    }\n\n    function renderColorPalette(r) {'
new_after_switch = '      }\n    }\n\n    function showCostEstimate(r) {\n      currentResult = r;\n      const snapshotEl = document.getElementById("snapshot");\n      currentResultImage = snapshotEl.src || "";\n      const saveBtn = document.getElementById("saveBtn");\n      const resultContent = document.getElementById("resultContent");\n      let html = "";\n      if (r.is_fallback) {\n        html = `<div class="fallback-banner"><span class="fallback-banner-icon">\\u2139\\ufe0f</span> Dit advies is algemeen \\u2014 voor een persoonlijke analyse specifiek voor jouw situatie hebben we een AI-verbinding nodig.</div>`;\n        resultContent.innerHTML = html;\n        return;\n      }\n      if (r.no_damage) {\n        html = `<div class="card" style="margin-bottom:14px"><div class="card-body" style="padding:24px;text-align:center"><div style="font-size:20px;font-weight:700;color:var(--sage);margin-bottom:8px">\\u2705 Geen schade</div><div class="res-desc">${r.message || "\\u2705 Geen schade geconstateerd."}</div></div></div>`;\n        resultContent.innerHTML = html;\n        return;\n      }\n      if (r.warning) {\n        html = `<div class="card" style="margin-bottom:14px"><div class="card-body" style="padding:24px;text-align:center"><div style="font-size:20px;font-weight:700;color:var(--terracotta);margin-bottom:8px">\\u26a0\\ufe0f Niet herkend</div><div class="res-desc">${r.warning}</div></div></div>`;\n        resultContent.innerHTML = html;\n        return;\n      }\n      // Kosten-first: compact diagnose + beide kosten direct\n      const issueType = r.issue_type || "Kosten Schatting";\n      const desc = r.description || "";\n      const diyCost = r.cost_diy || r.cost_range || "\\u2014";\n      const proCost = r.cost_pro || r.cost_range || "\\u2014";\n      html += `<div class="advice-section" style="margin-bottom:8px">`;\n      html += `<div class="identify-head" style="font-size:22px;margin-bottom:2px">${issueType}</div>`;\n      html += `<div class="identify-context" style="font-size:13px;margin-bottom:12px">${desc}</div>`;\n      // Cost comparison blocks - prominent, both shown\n      html += `<div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:16px">`;\n      if (diyCost !== "\\u2014") {\n        html += `<div style="padding:16px;border-radius:var(--radius-sm);background:var(--sage-soft);border:1px solid rgba(138,155,122,0.15);text-align:center"><div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.5px;color:var(--sage);margin-bottom:4px">Zelf doen</div><div style="font-size:22px;font-weight:800;color:var(--sage)">${diyCost}</div><div style="font-size:11px;color:var(--text-muted);margin-top:2px">alleen materiaal</div></div>`;\n      }\n      if (proCost !== "\\u2014") {\n        html += `<div style="padding:16px;border-radius:var(--radius-sm);background:var(--terracotta-soft);border:1px solid rgba(196,98,74,0.15);text-align:center"><div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.5px;color:var(--terracotta);margin-bottom:4px">Laten doen</div><div style="font-size:22px;font-weight:800;color:var(--terracotta)">${proCost}</div><div style="font-size:11px;color:var(--text-muted);margin-top:2px">incl. voorrijkosten</div></div>`;\n      }\n      html += `</div>`;\n      // Extra info in compact accordion-style\n      html += `<div style="display:flex;flex-direction:column;gap:6px">`;\n      if (r.steps && r.steps.length) {\n        html += `<details style="padding:10px 14px;border-radius:var(--radius-sm);border:1px solid var(--border-light);background:var(--bg-card)"><summary style="font-size:13px;font-weight:600;cursor:pointer;color:var(--text-primary)">Stappenplan</summary><div style="margin-top:8px;padding-top:8px;border-top:1px solid var(--border-light)">`;\n        r.steps.forEach((s, i) => { html += `<div style="display:flex;gap:8px;padding:4px 0;font-size:13px;color:var(--text-secondary)"><span style="flex-shrink:0;width:18px;height:18px;border-radius:50%;background:var(--border-light);display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:var(--text-muted)">${i+1}</span>${s}</div>`; });\n        html += `</div></details>`;\n      }\n      if (r.materials && r.materials.length) {\n        html += `<details style="padding:10px 14px;border-radius:var(--radius-sm);border:1px solid var(--border-light);background:var(--bg-card)"><summary style="font-size:13px;font-weight:600;cursor:pointer;color:var(--text-primary)">Materialen</summary><div style="margin-top:8px;padding-top:8px;border-top:1px solid var(--border-light);display:flex;flex-wrap:wrap;gap:4px">`;\n        r.materials.forEach(m => { html += `<span style="display:inline-block;padding:4px 10px;border-radius:6px;background:var(--bg-card);border:1px solid var(--border-light);font-size:12px;color:var(--text-secondary)">${m}</span>`; });\n        const gammaTips = (r.gamma_tips && r.gamma_tips.length) ? r.gamma_tips.join(", ") : r.materials.join(", ");\n        html += `</div><div style="margin-top:8px"><a class="gamma-link" href="https://www.gamma.nl/zoeken?q=${encodeURIComponent(gammaTips)}" target="_blank">Bestel bij Gamma</a></div></details>`;\n      }\n      if (providers && providers.length) {\n        html += `<details style="padding:10px 14px;border-radius:var(--radius-sm);border:1px solid var(--border-light);background:var(--bg-card)"><summary style="font-size:13px;font-weight:600;cursor:pointer;color:var(--text-primary)">Vakman in de buurt</summary><div style="margin-top:8px;padding-top:8px;border-top:1px solid var(--border-light)"><div class="filter-bar" id="provFilters" style="padding:0 0 8px 0"></div><div class="prov-grid" id="provGrid"></div></div></details>`;\n      }\n      html += `</div>`;\n      html += `<div style="height:40px"></div>`;\n      resultContent.innerHTML = html;\n      saveBtn.style.display = "none";\n      currentStep = "diagnose";\n      if (providers && providers.length) renderProviders();\n    }\n\n    function renderColorPalette(r) {'
s = s.replace(old_after_switch, new_after_switch)

with open(p, "w") as f:
    f.write(s)
print("done")